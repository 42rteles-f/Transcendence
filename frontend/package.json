{
  "name": "transcendence",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "prune": "rm -rf node_modules && rm -rf package-lock.json && npm cache clean --force",
    "re": "npm run prune && npm install && npm run build"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "@vitejs/plugin-basic-ssl": "^2.1.0",
    "typescript": "~5.7.2",
    "vite": "^6.2.0"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.8",
    "babylonjs": "^6.0.0",
    "jwt-decode": "^4.0.0",
    "socket.io-client": "^4.8.1",
    "tailwindcss": "^4.1.8"
  }
}
